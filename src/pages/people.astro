---
import Layout from "@/layouts/Layout.astro";
import PeopleProfileCard from '@/components/PeopleProfileCard.astro';
// Import the current test photo
import TestProfilePhoto from '@/assets/profile_pictures_sample.jpg'; 
// *** ADD THIS NEW IMPORT LINE ***
import maheshphoto from '@/assets/mahes.jpg';
import mahesh from '@/assets/m2.jpg'; 
// *******************************
import type { Person } from '@/types/people';
// Define the test data array
const people: Person[] = [
    {
        name: "Mahesh Cheedella ",
        title: "Research Collaborator",
        bio: "Mahesh Cheedella is a testing profile added to illustrate the layout for the People page. This profile demonstrates how to display collaborator information, including a bio and optional CV link. Please replace this with actual team member data.",
        // 2. Use the correctly imported asset variable here
        photoAsset: maheshphoto, 
        cvPath: `${import.meta.env.BASE_URL}pdfs/Mahesh Cheedella Resume.pdf`,
    },
    
    
    // Add more Person objects here for real profiles
];
---

<Layout title="People">
  <div class="prose max-w-none">
    <h1 class="text-3xl font-bold mb-8">People</h1>

    <section class="space-y-12 mb-12">
      <h2 class="text-2xl font-bold border-b pb-2">Research Team</h2>
      {people.map((person) => (
          <PeopleProfileCard person={person} />
      ))}
    </section>

    <div class="pt-8">
        <h2 class="text-2xl font-bold mb-4">Joining the Team</h2>
        <div class="card bg-base-100 border border-base-content/10">
            <div class="card-body">
                <p class="text-lg">If you are interested in joining the research group as a student or collaborator, please check the <a href={import.meta.env.BASE_URL} class="text-accent hover:underline">Open Positions</a> section on the Home page.</p>
            </div>
        </div>
    </div>

  </div>
</Layout>